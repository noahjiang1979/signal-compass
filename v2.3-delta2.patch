=== Signal Compass v2.3 Delta2 补丁 ===
日期: 2026-05-24
内容: H (SS交叉校验提前) + I (CY交叉校验修正[涨]/[跌]标签)
状态: qclaw 已打 D+B1(在 v2.3-delta.patch 中), 本补丁只含 H+I

=== 改动 1: H — SS 交叉校验提前到显示前 ===

位置: compass.py run_a() 函数, 核心指数矩阵输出之前 (原 L651)

原代码:
  print("  === A-Share Signal Map ===")
  risk=[];trend_score=0;...

改为 (在 print 之后、risk 之前插入交叉校验):
  print("  === A-Share Signal Map ===")
  # SS交叉校验提前到显示前 (v2.3: 防Sina实时错数据在输出层骗用户)
  _ss_sina_early=(q.get(A["SS"]) or {}).get("p")
  if _ss_sina_early is not None and syk and syk.get("c"):
    _ss_k_early=syk["c"][-1]
    if abs(_ss_sina_early-_ss_k_early)/_ss_k_early>0.01:
      if not is_trading():
        print("  ⚠️ SS数据源偏差: sina实时{:.2f} vs K线收盘{:.2f}, 展示用K线值".format(_ss_sina_early,_ss_k_early))
        _prev_close=syk["c"][-2] if len(syk["c"])>=2 else _ss_k_early
        _chg=(_ss_k_early-_prev_close)/_prev_close*100 if _prev_close>0 else 0
        q[A["SS"]]={"p":_ss_k_early,"chg":_chg}
  risk=[];trend_score=0;...

理由:
  原交叉校验在存档前执行(L1094), 但SS值在更早的L655已显示给用户.
  Sina实时 hq.sinajs.cn 对 sh000001 偶有收盘价错误(5/21: 4174 vs 实际4077), 导致:
  → 用户看到错误的 "+0.29%涨" 而非实际的 "-2.04%跌"
  → 基于错误显示写了 [涨] 的笔记
  → 第二天 "昨日笔记" 自然显示 [涨]
  本修复将校验提前到显示前, 盘中不触发(保留实时数据), 收盘后用K线值纠正.

验证逻辑:
  is_trading()=True (盘中) → 不触发, 保留实时数据
  is_trading()=False (盘后/收盘) → 偏差>1%时自动替换

=== 改动 2: I — CY 交叉校验修正 [涨]/[跌] 标签源 ===

位置: compass.py run_a() 函数, 存档段 auto 标签判定前 (原 L1061)

原代码:
  # --- 存档 ---
  cv=(q.get(A["CY"]) or {}).get("chg")
  if cv is not None:
    if cv>0.2:auto="[涨]"
    elif cv<-0.2:auto="[跌]"
    else:auto="[平]"
  else:auto=""

改为:
  # --- 存档 ---
  cv=(q.get(A["CY"]) or {}).get("chg")
  # CY交叉校验: Sina实时 chg vs K线实际日涨跌 (v2.3-I: 防[涨]/[跌]标签被脏数据骗)
  if cv is not None and cyk and cyk.get("c") and len(cyk["c"])>=2:
    _cy_k_chg=(cyk["c"][-1]-cyk["c"][-2])/cyk["c"][-2]*100
    if abs(cv-_cy_k_chg)>1 and not is_trading():
      print("  ⚠️ CY涨跌偏差: sina实时{:+.2f}% vs K线实际{:+.2f}%, 标签用K线值".format(cv,_cy_k_chg))
      cv=_cy_k_chg
  if cv is not None:
    if cv>0.2:auto="[涨]"
    elif cv<-0.2:auto="[跌]"
    else:auto="[平]"
  else:auto=""

理由:
  [涨]/[跌]/[平] 自动标签是用 sina_q 创业板实时涨跌判定的.
  标签存入 review JSON 的 note 字段, 被以下消费端读取:
  - calc_accuracy() 复盘准确率
  - calc_narrative_accuracy() 叙事验证
  - get_yesterday_note() 昨日笔记显示
  若 Sina 创业板数据也出错, 整个标签链全错.
  修复: 盘后用 sina_k("sz399006","250") 的日K线计算实际涨跌, 偏差>1%时替换.
  cyk 变量已在 L619 拉取(创业板250天K线), 无需额外请求.

验证逻辑:
  同 H: 盘中保留实时数据, 盘后偏差>1%自动纠正.
  今天测试未触发(数据一致), 代码就绪待触发.

=== 补丁应用说明 ===
1. 本补丁只含 H+I, qclaw 已打过 D+B1 的不重复
2. H 和 I 可独立验证: 盘中跑不触发, 盘后 Sina 报错日才触发
3. 两者都依赖已有变量(syk/cyk), 无新增网络请求
