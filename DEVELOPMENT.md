# Hermes Signal Compass — 开发者手册

## 一、项目定性

双模式 A 股 / 美股指数的**免费数据信号监控系统**，面向做空/对冲决策辅助。

- 不是量化回测平台，不生成交易信号
- 不做自动化（双击 .bat 触发）
- 输出目标：终端，表格 = CJK 等宽硬对齐
- 语言：中文

## 二、技术栈

| 层 | 选型 | 版本 | 说明 |
|----|------|------|------|
| 运行时 | Python | 3.12+ | 标准库 + pip 包，无虚拟环境要求 |
| HTTP | requests | 最新 | 唯一网络库 |
| 金融数据 | akshare | 最新 | 主要数据源（东财接口） |
| 搜索引擎 | ddgs | 最新 | DuckDuckGo CLI，用于叙事信号 |
| 表格对齐 | 自研 | cjk_table.py | CJK 感知对齐（wcwidth），中文=2 ASCII=1 |
| 启动器 | Windows .bat | — | 桌面双击，不用 cron |

**依赖安装**（用户机器已装，新人需要）：

```bash
pip install requests akshare ddgs
```

## 三、文件结构

```
```
signal_compass/
├── compass.py           # 核心文件，四模式（~1850 行, v2.1+叙事模块后~2000行）
├── cjk_table.py         # CJK 表格对齐（wcwidth 封装）
├── compass.bat          # 统一启动器：菜单选择 US/A/Review/组合池
├── portfolio.json       # 自选组合池配置文件（手动创建修改）
├── AGENTS.md            # 项目状态快照（跨会话恢复用）
├── DEVELOPMENT.md       # 本文件
├── 10y_cache.json       # 中国 10Y 国债日级缓存（自动生成）
├── dq_state.json        # 数据质量退化状态持久化（v2.2，自动生成）
├── review/              # 复盘存档目录（自动生成）
│   ├── YYYYMM/          # v2.1: 按月分目录
│   │   ├── YYYYMMDD.json      # 当日总索引
│   │   ├── YYYYMMDD_HHMM.json # 时段快照（如_0915, _1130, _1505）
│   │   └── ...
│   ├── narratives/      # v2.1: 叙事追踪
│   │   ├── patterns.json      # 叙事模板库（条件→预判→命中率）
│   │   ├── YYYYMMDD.json      # 当日触发的叙事+验证窗口
│   │   └── ...
│   └── signals/         # v2.1: 已验证信号
│       └── validated.json     # 命中率≥65%后升级的信号
└── DELIVERY_REVIEW_*.md # 交付审核快照（一次性，非开发者手册）
```

## 四、架构设计

### 4.1 模块划分（按行号，v2.0基准）

| 行号 | 内容 | 说明 |
|------|------|------|
| 1-15 | 常量定义 | 股票代码映射、请求头、Sina URL、静态满分常量 |
| 16-39 | 工具函数 | `now()`, `hdr()`, `dma()`, `rsi()`, `pct_rank()`, `market_amount()`, `sina_k()`, `sina_q()`, `yahoo_c()` |
| 39-88 | 缓存工具 + 数据获取 | northbound_cache 读写、`north_b()`、`china_10y()`、`get_zt_data()`、`yc()` |
| 73-170 | Sina + Yahoo 工具 | 实时行情、K 线、收益率曲线 |
| 171-179 | DDG 叙事 | Burry 相关新闻搜索 |
| 180-440 | `run_us()` | US 模式主体 |
| 441-850 | `run_a()` | A 股模式主体 |
| 851-1000 | Portfolio模式 | 自选组合池 |
| 1001-1100 | 复盘 + 入口 | `run_review()`、命令行分发 |
| 1100+ | v2.1新增 | 叙事模块、slot参数、多时段存档（见第十二章） |

### 4.2 数据源映射

#### US 模式

| 数据 | 函数 | 源 | 可靠性 | 备选 |
|------|------|----|--------|------|
| 指数/ETF 实时 | `sina_q()` | Sina 实时行情 `hq.sinajs.cn` | 🟢 极高 | 无 |
| SPY K 线 | `yahoo_c()` | Yahoo Finance `query1.finance.yahoo.com` | 🟡 中等 | 无 |
| 收益率曲线 | `yc()` | Yahoo `^TNX`+`2YY=F` → `^FVX` fallback | 🟡 中等 | 双路径内建 |
| 叙事信号 | `ddg()` | DuckDuckGo CLI | 🟠 辅助 | 无，取不到显示[无] |

#### A 股模式

| 数据 | 函数 | 源 | 可靠性 | 备选 |
|------|------|----|--------|------|
| 指数实时 | `sina_q()` | Sina `hq.sinajs.cn` | 🟢 极高 | 无 |
| 成交量 | `sina_k()` | Sina 日 K 线（30 天） | 🟢 高 | 无 |
| 北向资金 | `north_b()` | AkShare `stock_hsgt_fund_flow_summary_em` | 🟡 中等 | 无（盘中延迟 10-15 分钟） |
| 10Y 国债 | `china_10y()` | AkShare `bond_zh_us_rate` + 日级缓存 | 🟡 中等 | 无（首次慢，后续缓存秒出） |
| 涨停板 | `get_zt_data()` | AkShare `stock_zt_pool_em` | 🟡 中等 | 无（盘中约 10 分钟更新） |
| 涨跌家数 | 内联请求 | 东财 push2 API | 🟠 不稳定 | try-except 保底 |
| 恒指 | `yahoo_c()` | Yahoo `^HSI` | 🟡 中等 | 无 |

### 4.3 缓存策略

所有缓存都是 JSON 文件，同名目录下直接读取，**零外部依赖**。

| 缓存文件 | 策略 | 过期规则 |
|----------|------|----------|
| `northbound_cache.json` | 每次 `north_b()` 成功写入，`get_nb_3d()` 读取最近 3 个交易日 | 7 天前自动清理 |
| `10y_cache.json` | 每日首次拉取后缓存，后续命中直接返回 | 日期不匹配时自动刷新 |

## 五、信号系统详解

### 5.1 US 模式

**三层判定：**

1. **数据层**：SPY 价格、RSI、20MA/50MA/200MA、成交量比、Mag7 分化度、板块轮动差、VIX、收益率利差
2. **加权评分**（满分 15 分）：
   - SPY 收跌 +1 | 跌破 20MA +1.5 | 跌破 50MA +2
   - 成交量 >1.2x +1 | RSI>70 +1 | RSI>80 +1.5
   - Mag7 跌幅 >5 家 +2 | 标准差 >2% +1
   - SOX 跑输 QQQ>2% +1.5 | XLE 跑赢 >2% +1
   - IWM 跑输 >0.5% +1 | VIX>25 +1
   - 利差 <0.5% +1 | Burry 叙事 +0.5
3. **判定**：<5 🟢 乐观 / 5-10 🟡 注意 / >=10 🔴 警戒 → 15 分时确认做空

**升级/降级**：满足 ≥3 项升级条件（跌破 20MA / 跌破 50MA / 放量 20%+ / Mag7 普跌 ≥5 / VIX>30）转为确认做空窗口

### 5.2 A 股模式

**五层判定：**

1. **数据层**：6 指数矩阵、涨跌家数比、成交量比、板块轮动差（银行/半导体/上证50/医药 vs 科技）、主力资金流向、北向资金（单日+3日累计）、10Y 国债、涨停分布
2. **加权评分**（满分 12 分）：
   - 普跌: 每 1 指数跌 +1（上限 6）
   - 涨跌家数比 >3:1 +1.5 | >5:1 +2.5
   - 主力净流出 >150 亿 +2 | >100 亿 +1
   - 北向净流出 >100 亿 +1.5 | >50 亿 +1；3 日累计 <-150 亿 +2 | <-100 亿 +1.5
   - 10Y 国债 >2.0% +1
3. **判定**：<=3 🟢 中性 / 4-6 🟡 注意 / 7-9 🔴 警戒 / 10-11 🛑 高风险 / 12 🚨 确认调整

**升级/降级**：创业板跌破 20MA / 涨跌比 >5:1 / 缩量 <0.8x / 北向 3 日累计流出 >150 亿 / 主力单日 >200 亿 / 10Y>2.0% — ≥3 项升为高风险

### 5.3 异常提示系统

机器只做异常检测，不写叙事。模板固定为：

```
→ 分析:
  ✅ 确认信号 / ⚠️ 观察信号
💡 今日观察: [动态选一条]
💡 我的笔记: _________________________
```

异常检测规则写死在每个模式的 if 链中，修改需要改代码。

## 六、关键设计决策

1. **20MA 而非 MACD**：更直观，A 股短线资金实际参考线，滞后性更低
2. **Sina K 线而非 Yahoo 成交量**：Yahoo A 股成交量跳动 19x，Sina 稳定
3. **北向资金 = 沪股通 + 深股通成交净买额之和**：AkShare 接口单列汇总
4. **10Y-2Y 收益率差而非 10Y-3M**：2YY=F 数据稳定，^IRX 不稳定
5. **板块轮动 = 涨跌幅差而非价格比**：差值更直观反映资金流向变化
6. **双模式启动**：桌面双击bat手动运行 + OpenClaw cron定时推送（compass.py本身仍支持手动双击，cron只是自动触发）
7. **不输出确定性叙事**：机器只标记异常用问句引导，结论由人类写
8. **表格=标签，提示=详情**：事件类信息在表格中只显示关键词短标签（≤6字），完整描述下沉到提示区。避免截断和冗余，确保一眼扫过全是干货
9. **三层架构解耦**：数据层/信号层/表达层独立，AI断线时compass.py仍可独立运行（v2.1新增）
10. **去Yahoo依赖**：A股模式全量国内源，US模式降级为影响因子，不再依赖梯子（v2.1新增）
11. **叙事=待验证假说**：叙事不等于信号，需20+样本且命中率≥65%才能升级为信号（v2.1新增）
12. **多时段存档**：同一指标在不同时段的差异本身就是信号维度（v2.1新增）
13. **数据质量门控**：字段缺失不应静默忽略，而应结构化暴露——scope reduction而非binary gate。缺失分critical/important两层：critical缺→减范围辩论，critical+important同缺→禁辩论（v2.2新增）
14. **约束倒推**：设计结构性约束时，必须从最硬层(Python代码)→结构化锚点(JSON字段)→Prompt清单→软备忘录。禁止从最软层开始设计（v2.2新增）

## 七、数据源可靠性评级

```
🟢 极高 — Sina 实时行情 (hq.sinajs.cn)
    连续运行 6 个月零故障，反爬极松，延迟 <1s
    故障模式：返回空字符串 ""，需 try-except

🟡 中等 — Yahoo Finance、AkShare
    Yahoo：VPN 断开时 404，梯子稳定时才可用
    AkShare：依赖东财后端，改版偶发失效（概率 <5%）

🟠 不稳定 — 东财 push2 API
    push2.eastmoney.com 的 K 线接口 (Sina K 线已替代)
    涨跌家数接口偶有 format 变化（list ↔ dict 自动适配）
```

## 八、扩展指南

### 8.1 新增一个信号指标

```
1. 在数据获取阶段（run_us 或 run_a 开头）调对应接口
2. 在加权评分块中加 if 判断和得分
3. 在升级/降级条件块中加条件打印
4. 如需异常检测，在 → 分析块中加 if 链
```

### 8.2 新增一个数据源

```
1. 在文件顶部写一个新函数（如 sina_k、yahoo_c 模式）
2. 返回值保持统一格式：成功返回数值/字典，失败返回 None
3. 不要在数据获取函数内部做判定逻辑
4. 加 try-except，失败时打印到 stderr 或静默返回 None
```

### 8.3 新增一个模式

```
1. 写一个新函数 run_xxx()
2. 在 __main__ 入口加 elif 分发
3. 参考 run_us 和 run_a 的结构：
   数据获取 → 信号映射 → 加权评分 → 判定 → 异常分析 → 今日观察 → 升级/降级条件
```

### 8.4 修改判定阈值

阈值全部写在对应 if 语句中，没有集中配置。搜索 `+1`、`+1.5`、`+2` 等关键词可找到所有评分规则。

## 九、常见问题

**Q: 北向资金显示"[盘中数据延迟]"是什么情况？**
A: AkShare 接口盘中延迟 10-15 分钟，13:00 后曲线恢复正常。收盘后 15:30 数据最准确。

**Q: 10Y 国债为什么这么慢？**
A: 首次运行时 akshare `bond_zh_us_rate` 需要下载整个历史表（约 30 秒）。之后日级缓存命中，秒出。

**Q: US 模式的成交量比用的什么基线？**
A: 20 日均量（来自 Yahoo 1 年 K 线数据）。

**Q: 为什么没有备选数据源？**
A: 经过全面验证，A 股的北向资金/10Y 国债实时接口（Sina、腾讯、东财 push2）均无法稳定获取。保持 AkShare 唯一源 + try-except 兜底，比加无效 fallback 代码更优。

**Q: 怎么加自己的指标？**
A: 见第八章扩展指南。核心原则：数据获取和判责分离。

**Q: v2.1 的 --slot 参数是必须的吗？**
A: 不传时行为与 v2.0 完全一致。只有定时任务才需要指定时段。

**Q: 叙事模板怎么新增？**
A: 编辑 `review/narratives/patterns.json`，新增一个 tag 即可。条件字段支持：signal（信号文本包含）、chg（涨跌幅阈值）、events（事件关键词）、rel_strength（相对大盘）、vr（量比）、signal_change（信号切换如🔴→🟡）。详见第十二章。

**Q: 叙事命中率多少才能升级为信号？**
A: n≥20 且命中率≥65%。50-65%保留为观察级，<50%(n≥20)淘汰。

**Q: US 模式还有用吗？**
A: v2.1 后 US 模式降级为"影响因子"——早上看一次美股收盘对 A 股开盘的影响即可。已去除 Yahoo 依赖，改用 akshare 国内源。近期无美股交易工具时不需要完整技术面。

## 十、复盘功能（Review Mode）

### 10.1 设计理念

复盘是 compass.py 在**时间维度上的自然延伸**。现有框架解决了"今天的数据怎么看"，复盘解决"今天的数据和昨天比有什么变化"。

核心分工：
- 机器做：自动存档 → 趋势展示 → 异常窗口标记
- 人做：补充笔记 [涨]/[跌]/[平] 标签 → 判断信号有效性

### 10.2 三层架构

| 层级 | 触发方式 | 功能 |
|------|---------|------|
| 第一层：自动存档 | 每次运行自动触发 | 主流程末尾调用 `save_daily_review()`，将当日关键信号存入 `review/YYYYMM.json` |
| 第二层：复盘报告 | `python compass.py review` 或选中 compass.bat 菜单 3 | 读取最近 30 天数据，输出得分趋势线 + 异常窗口 + 笔记回顾 |
| 第三层：信号闭环 | 手动在笔记中标注 | 笔记里写 `[涨]` / `[跌]` / `[平]`，`calc_accuracy()` 自动统计命中率 |

### 10.3 自动存档规则

- 存档位置：`review/YYYYMM.json`（按月分文件）
- 单日去重：同一天多次运行只保留最后一次
- 存储内容：模式、得分、满分、风险等级、关键指标快照、分析摘要、观察、笔记
- 每次约 1KB，一年不到 400KB

### 10.4 笔记联动

- 每次运行主模式时，顶部自动显示**前一天**的笔记（如果存在）
- review 模式中按时间线串起所有笔记
- 标记方式：在笔记中写 `[涨]` 或 `[跌]` 或 `[平]`，供未来命中率统计使用

### 10.5 技术实现要点

```python
# 存档（运行即触发，无感）
save_daily_review({"mode":"us","score":score,"max":max_score,...})

# 显示昨天笔记
yn=get_yesterday_note()
if yn: print("  🖋 昨日笔记：{}".format(yn))

# 底部提示
print("  🧑 双击 compass.bat 或选菜单 3 看历史趋势")
```

### 10.6 注意事项

- 第一次运行 review 模式时，如果数据不足 2 天会提示"还没有复盘数据"
- 趋势线的 `fixed_max` 参数固定为当前模式的满分（US=15, A=12），确保不同天数的趋势线可比
- 异常窗口的阈值（默认连续 3 天升降）可调整 `detect_windows(scores, threshold=N)` 参数
| - 命中率计算依赖人工标注，标注前返回 None

## 十一、Portfolio 模式（自选组合池）

### 11.1 功能概述

`python compass.py portfolio` 对自选池每只股票独立计算组合信号（MACD+BOLL+KDJ），结合事件检测和异常偏离检测，输出相对大盘强弱矩阵。

### 11.2 数据流

```
portfolio.json → 代码列表
sina_q() → 实时行情 → 相对大盘（基准: 上证+科创50+SPY）
sina_k(code, 35) → 35天日K线 → get_signal() → 组合信号
check_events() → 东财直连事件 → 短标签（表格）+ 长描述（提示区）
count_anomaly_ann() → 异动公告次数（近3月）
```

### 11.3 表格列定义

| 列 | 内容 | 来源 |
|----|------|------|
| 代码 | 股票简称（前4字） | Sina 实时行情 name |
| 价格 | 当前价 | sina_q() price |
| 涨跌 | 当日涨跌幅 | sina_q() chg |
| 相对大盘 | 个股涨跌 - 上证涨跌 | 计算 |
| 观察 | 🟢🟡🔴 + 强弱标签 | rel 阈值（>1→⭐抗跌, >0.5→抗跌, >-0.5→跟跌, else→领跌） |
| 信号 | 组合信号 emoji + 文字 | get_signal() 输出 |
| 事件 | 标签化事件（🔒质押等）或 🟡异常偏离 | check_events() tag_str / anomaly_tag |

### 11.4 事件系统

详见十二章。核心原则：**表格=标签，提示=详情**。

### 11.5 颜色覆盖

当「信号」列触发 🔴严重异动/预警/回避 时，观察栏颜色强制降级：
- `col`：🟢 → 🟡
- `hint`：去掉 ⭐ 前缀（"⭐抗跌" → "抗跌"）

### 11.6 异常偏离检测

- 交易所标准：连续3日累计涨幅≥20%
- 表格显示：🟡异常偏离
- 提示区显示：交易所要求发异动公告 | 近3月触X次（不重复标签）

### 11.7 提示区排序优先级

1. 🔴严重信号（含质押/减持双重风险）→ 附注：异动公告历史 + 板块分析
2. 事件详情（长描述，严重信号的下级子项）
3. 🟡异常偏离 → 交易所要求发公告
4. 利好出尽（👍事件 + 领跌）
5. 仅事件（利好/中性事件展示）
6. 弱信号

## 十二、v2.1 架构升级（2026-05-22）

### 12.1 升级背景

v2.0及之前版本存在三个根本性问题：

1. **Yahoo单源故障**：US模式的SPY技术面、A股模式的上证MA/RSI均依赖Yahoo Finance API，需要梯子稳定。断梯时程序能跑但残缺（实时行情来自国内源，技术指标全丢）。
2. **无多时段追踪**：每天只跑一次，午盘和收盘数据差异巨大（如5/22主力资金-7亿→+127亿），单次快照无法捕捉日内变化。
3. **叙事不可验证**：第四节锐评含叙事推理，但写完就消失，无法回测命中率，无法沉淀为可复用的信号。

### 12.2 v2.1 架构分层

采用业界标准的Alpha Research Stack三层架构：

```
┌─────────────────────────────────────────┐
│  表达层（AI）                            │
│  新闻解读 · 报告撰写 · 交互式问答         │
│  ← 可以断，不影响下面                     │
├─────────────────────────────────────────┤
│  信号层（规则引擎）                       │
│  compass.py · 叙事模板 · 命中率统计       │
│  ← 零AI，纯Python，双击即跑              │
├─────────────────────────────────────────┤
│  数据层（管道）                           │
│  sina_q · akshare · 东财API · 缓存       │
│  ← 最底层，断了什么都做不了               │
└─────────────────────────────────────────┘
```

**关键原则**：每层只依赖下一层，不越层依赖。表达层断了，信号层照样出数据；信号层挂了，数据层照常抓。

### 12.3 数据层改造：去Yahoo依赖

#### A股模式

| 数据 | v2.0源 | v2.1源 | 状态 |
|------|--------|--------|------|
| 上证MA/RSI | yahoo_c("000001.SS") | ak_daily("sh000001") → yahoo_c fallback | ✅完成 |
| 沪深300 RSI/MA | yahoo_c("000300.SS") | ak_daily("sz399300") → yahoo_c fallback | ✅完成 |
| 恒指价格/涨跌 | yahoo_c("^HSI") | sina_q("hkHSI") 实时 → yahoo_c fallback | ✅完成 |
| 创业板RSI分位 | sina_k("sz399006",250) | 同左 | 不变 |

#### US模式

| 数据 | v2.0源 | v2.1源 | 状态 |
|------|--------|--------|------|
| SPY实时价 | sina_q("gb_spy") | 同左 | 不变 |
| SPY K线(1y/5y) | yahoo_c("SPY") | ak_daily("SPY") → yahoo_c fallback | ✅完成 |
| VIX | yahoo("^VIX") | sina_q("gb_vixy") 近似 | 不变 |
| 国债利差 | yc() → Yahoo | 暂时优雅跳过 | 待后续补国内源 |

**效果**：A股模式完全去Yahoo化，US模式核心指标(SPY技术面)已去Yahoo。yahoo_c保留为fallback，国内源失败时自动降级。

### 12.4 信号层改造：多时段存档

#### 存档结构升级

v2.0：`review/202605.json` 按天一条，扁平结构。

v2.1：按天+时段分层，支持日内对比：

```
review/
├── 202605/
│   ├── 20260522.json          # 当日总索引
│   ├── 20260522_0915.json     # 早盘快照
│   ├── 20260522_1130.json     # 午盘快照
│   ├── 20260522_1505.json     # 收盘快照（权威版）
│   ├── 20260522_2000.json     # 晚间快照
│   └── ...
├── narratives/
│   ├── patterns.json          # 叙事模板库
│   ├── 20260522.json          # 当日触发的叙事
│   └── ...
└── signals/
    └── validated.json         # 已验证信号
```

#### 新增命令行参数

```bash
python compass.py a --slot 1130     # 午盘快照，存为20260522_1130.json
python compass.py a --slot 1505     # 收盘快照（默认）
python compass.py portfolio --slot 1130
```

不传slot时默认为当前时段整点（如11:30运行→slot=1130）。

#### 时段差值信号（新增）

积累多时段数据后，可计算"午后动量"：

```python
delta_main = close_main - noon_main    # +134亿 = 午后主力流入
delta_limit = close_limit - noon_limit  # +27只 = 尾盘情绪升温
delta_ratio = close_ratio - noon_ratio  # 涨面扩大
```

这些delta可沉淀为独立信号维度。

### 12.5 叙事系统：从假说到信号

#### 核心思路

叙事=人写的判断规则，需要验证后才能成为信号。v2.1引入标准化的叙事追踪流程。

#### 叙事模板库（patterns.json）

每个叙事模板包含：

```json
{
  "tag": "拉高出货",
  "conditions": [
    {"field": "signal", "op": "contains", "value": "偏空"},
    {"field": "chg", "op": ">", "value": 3},
    {"field": "events", "op": "contains_any", "value": ["减持完毕","减持计划"]}
  ],
  "prediction": "T+1~T+3 跑输基准指数",
  "window": 3,
  "stats": {"total": 0, "hit": 0, "miss": 0, "rate": null}
}
```

#### 叙事生命周期

```
[假设阶段] 条件定义 → 匹配触发 → 存档narratives/
    ↓ n≥20样本
[验证阶段] calc_narrative_accuracy() → 统计命中率
    ↓ 命中率≥65%          ↓ <50%
[信号阶段] 升级为validated信号   [淘汰] 删除或修正条件
```

#### 预置叙事模板（v2.1）

| 叙事标签 | 触发条件 | 预判 | 验证窗口 |
|---------|---------|------|---------|
| 拉高出货 | 偏空 + 减持完毕 + 日涨>3% | T+1~T+3跑输大盘 | 3天 |
| 利好不涨 | 有利好事件 + 相对大盘<0 | T+1~T+3继续弱 | 3天 |
| 虚涨翻多 | 信号日内🔴→🟡 + 日涨>5% | T+1~T+3回吐≥50% | 3天 |
| 板块绑架 | 板块差>-4% + 中性评分 | 3日内科技回调→评分跳升 | 3天 |
| 缩量抗跌 | 偏空 + 相对大盘>1% + 量比<0.85 | T+1~T+3补跌概率>60% | 3天 |

### 12.6 表达层：AI报告生成

#### 职责分工

| 模块 | compass.py（信号层） | AI（表达层） |
|------|---------------------|-------------|
| 数据抓取 | ✅ | ❌ |
| 信号计算 | ✅ | ❌ |
| 叙事匹配 | ✅ 规则引擎 | ❌ |
| 新闻解读 | ❌ | ✅ 需AI在线 |
| 报告撰写 | ❌ | ✅ 需AI在线 |
| 第四节锐评 | ❌ | ✅ 含叙事推理 |

#### 综合报告流程

```
15:05  compass.py a+pf → review/20260522_1505.json
15:10  AI读取JSON + 抓今日新闻 → 生成综合报告
15:15  推送给用户
```

新闻源建议：财联社电报（免费API）、东方财富要闻、同花顺7x24。都是国内源，无需梯子。

#### 报告频率建议

| 时段 | 推送 | 用途 |
|------|------|------|
| 09:00 | ❌ | 早盘前快照，仅存档用于累积 |
| 11:30 | ✅ | 午盘报告，给你看盘中预判 |
| 15:05 | ✅ | 收盘报告（最准），权威存档 |
| 20:00 | ✅ | 晚间综合，含美股影响+当日复盘 |

**每天看3次**（午盘/收盘/晚间），系统跑4次（多一次早盘前用于积累）。

### 12.7 定时任务配置

使用OpenClaw的cron工具配置4个任务（工作日触发，微信推送）：

| 任务 | 时间 | cron表达式 | 执行内容 |
|------|------|-----------|----------|
| US收盘简报 | 09:00 | `0 9 * * 1-5` | compass.py us --slot 0900，叙事+validated信号 |
| A股午盘 | 11:00 | `0 11 * * 1-5` | compass.py a+pf --slot 1130，叙事匹配 |
| A股收盘 | 15:05 | `5 15 * * 1-5` | compass.py a+pf+review --slot 1505，叙事回验+validated |
| 晚间深度 | 23:00 | `0 23 * * 1-5` | compass.py us+a+review --slot 2300，双市场+回验 |

> **注**: 叙事回验(calc_narrative_accuracy+check_narrative_promotion)已集成到review模式中，无需单独cron任务。每次review自动执行回验统计+升降级判定。

### 12.8 代码改动清单（v2.0 → v2.1）

| 改动项 | 行号（预估） | 说明 | 状态 |
|--------|-------------|------|------|
| sina_k扩展支持上证250天 | L120-130 | 去Yahoo依赖 | ✅已完成 |
| ak_daily()新增函数 | L355-387 | akshare国内源K线，替代yahoo_c | ✅已完成 |
| sina_q加hkHSI解析 | L323-325 | 恒指实时数据国内源 | ✅已完成 |
| A股sy/hs改ak_daily优先 | L651-652 | yahoo_c降为fallback | ✅已完成 |
| A股hsi改sina_q优先 | L653-658 | yahoo_c降为fallback | ✅已完成 |
| US模式yi/yi5改ak_daily | L411 | SPY技术面国内源 | ✅已完成 |
| --slot参数解析 | L1960+ | 命令行参数 | ✅已完成 |
| 存档函数改造 | save_daily_review() | 多时段slot+日索引+向后兼容 | ✅已完成 |
| 叙事模板加载 | L203-210 | load_patterns() | ✅已完成 |
| 叙事条件匹配 | run_a()/run_us()末尾 | match_narratives()+_check_cond() | ✅已完成 |
| 叙事命中率统计 | calc_narrative_accuracy() | 复盘模式扩展 | ✅已完成 |
| 叙事升降级 | check_narrative_promotion() | 升级/淘汰/降级 | ✅已完成 |
| validated信号 | apply_validated_signals() | A+US双模式+2分上限 | ✅已完成 |
| yc()国债利差改造 | akshare优先+Yahoo fallback | 东财bond_zh_us_rate | ✅已完成 |
| cron定时任务 | 4任务delivery修复 | 微信channel路由 | ✅已完成 |

已新增代码：+210行（P0: +35行, P1: +95行, P2: +80行）。

### 12.9 兼容性说明

- **向后兼容**：不传--slot时自动推断时段(09:00/11:30/15:05/20:00)，行为与v2.0一致
- **存档兼容**：v2.0的review/YYYYMM.json仍被写入，v2.1新增review/YYYYMM/YYYYMMDD_SLOT.json快照
- **降级运行**：AI层断开时，compass.py仍可独立运行输出终端表格
- **叙事文件**：patterns.json放narratives/目录，5个预置模板已初始化
- **回验引擎**：review模式自动统计叙事命中率+升降级，patterns.json的stats字段由引擎维护
- **validated信号**：signals/validated.json，升级条件n>=20且rate>=0.65，最多贡献+2分
- **yc()数据源**：akshare东财bond_zh_us_rate优先，Yahoo降为fallback，US模式收益率差不再[无数据]
- **cron路由**：4任务全部announce→openclaw-weixin，delivery正常

---

## 十三、事件系统详解

### 12.1 架构

```
check_events(code, name, days=30)
  → 东财直连API np-anotice-stock.eastmoney.com
  → 分类: zc(质押) / qy(减持/诉讼/立案/冻结/处罚/违规/亏损) / ba(增持/回购/中标/预增/扭亏)
  → 否定前缀过滤: "未/无/不"开头的关键词不标利空
  → 返回 (emoji, 纯emoji, 短标签, 长描述)

_make_event_tag(title, typ)
  → 关键词→短标签（≤6字）
  → zc: 解质/质押延期/质押
  → qy: 减持完毕/减持计划/诉讼/立案/冻结/预亏
  → ba: 中标/增持/回购/预增/扭亏
```

### 12.2 三版本输出

| 版本 | 用途 | 示例 |
|------|------|------|
| 纯emoji | （保留，暂未使用） | "🔒👎" |
| 短标签 | 表格事件列 | "🔒质押 \| 👎减持完毕" |
| 长描述 | 提示区事件详情 | "🔒控股股东解除质押 | 👎部分董事减持计划完成.." |

### 12.3 缓存

- `EVT_CACHE`：进程内缓存，key = `{代码}_{天数}`，避免重复请求
- `EVT_LONG_DETAILS`：长描述映射字典，供提示区查找

### 12.4 数据结构

```python
# check_events 返回
("⚡",           # emoji（历史兼容）
 "🔒👎",         # 纯emoji
 "🔒质押 | 👎减持完毕",  # 短标签
 "🔒控股股东解质 | 👎部分董事减持完成")  # 长描述

# 缓存存储
EVT_CACHE[key] = ("⚡", short_str, tag_str, long_str)
EVT_LONG_DETAILS[lbl] = long_str  # 提示区使用
```



---

## 十四、v2.3 信号地基修复（2026-05-24）

### 14.1 升级背景

三方评审（Hermes + qclaw + WorkBuddy）对 `get_signal()` 全量盘点后发现四个地基级缺陷：

1. **K线窗口不足**：`sina_k(code, 35)` → MA60永远为None → MA趋势分支从未执行
2. **BOLL未实现**：docstring写"MACD+BOLL+KDJ"但无BOLL代码
3. **KDJ13白算**：算出来但无引用，震荡分支只用了KDJ(9)
4. **cap标签白算**：按成交额估算大盘/中盘/小盘，但无分支使用

### 14.2 Step 1: K线窗口 35→250

```python
# 原: kd=sina_k(code,35)
# 改: kd=sina_k(code,250)
```

MA5/10/20/60/120 全活。上证/创业板 RSI 分支已在用 250 天，接口无需验证。

### 14.3 Step 2: KDJ13 双确认

震荡分支改用 KDJ(9) + KDJ(13) 方向一致才出信号。不一致时落 MACD 方向判定。

Feature Flag: `_F_K13=True`

### 14.4 Step 3: BOLL 布林带

新增 `_boll_calc(c, n=20)` — 中轨±2σ。位置标签 [上轨]/[中轨上方]/[中轨下方]/[下轨]。带宽收窄检测（20日带宽处于最低20%分位）→ 强多头/偏多降一级，追加 [收窄] 标签。

Feature Flag: `_F_BOLL=True`

### 14.5 Step 4: cap 标签

[大盘]/[中盘]/[小盘] 纯信息标签（日均成交额>50亿/10-50亿/<10亿）。

Feature Flag: `_F_CAP=True`

### 14.6 D: 收窄降级 rsc 数值匹配

`rtxt=="强多头"` → `rsc>=3`，字符串→数值匹配。

### 14.7 B1: 缩量标签

`vr<0.7 & rsc>=1` 时追加 [缩量] 标签，空头缩量不标。

### 14.8 H+I: SS/CY 交叉校验

SS: Sina实时 vs K线收盘偏差>1%时自动用K线值替换显示。
CY: [涨]/[跌]标签源盘后与K线交叉校验，偏差>1%纠正。

---

## 十五、v2.4 信号增强 + 100只回测（2026-05-24）

### 15.1 背景

WorkBuddy 的协创数据深度回溯 + 10只股票回测暴露问题 → PRD 六项要求。
267点回测（98只×3时间点，Baostock前复权）验证。

### 15.2 H-13 市场环境感知 + 牛调

`_get_market_regime(ba,cy,st50)` 读 review 存档/3指数快照判定牛熊。
牛调: mkt≥1且rsc≤-1→rsc+1，不跨0翻牌。
Hotfix: `_mkt==0` 时硬编码 mkt=1(偏牛)。

### 15.3 H-6 成交量否决

`vr<0.7 & rsc>=2` 缩量涨降一级。B1 已处理 [缩量] 标签，不重复。

### 15.4 H-14 急涨/超拔标签

`chg_20d>50→[急涨]` / `vs_ma20>25→[超拔]`，纯信息。

### 15.5 H-15 反弹无力（含 Bug 修复）

`p<hi20*0.95 & vs_ma20>-5 & rsc>=0→[无力]降一级`。

**Bug修复**: 原 `max(rsc-1,-1)` 在 rsc<=-2 时翻转方向——rsc=-2→max(-3,-1)=-1，从"偏空"升级到"偏弱"（75次误升级）。加 `rsc>=0` 守卫，WorkBuddy 从统计数据反推发现。

### 15.6 H-1 翻译引擎

`_translate_signal(rsc, sig_txt)` 14条规则，rsc+标签→一句话操作建议。供 cron 消费。

### 15.7 267点回测结果

数据源: Baostock (前复权，零限流)

  r≥2 偏多胜率: 35% → 39% (+4%)
  r≤-2 偏空胜率: 46% → 46% (不变)
  [急涨] 4次, 均收益 +9.9%
  [超拔] 8次, 均收益 +8.8%
  [无力] 12次, 均收益 -2.2% (修复后)

### 15.8 待办

- H-13 完整版: 去掉 mkt 硬编码
- Task C: Baostock ak_daily 封装 (前复权K线，已验证可行)
- Phase 3.0: vectorbt 参数网格扫描 (阈值50%/25%/0.95均为经验值)
