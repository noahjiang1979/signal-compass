=== Signal Compass v2.3 Delta 补丁 ===
日期: 2026-05-24
内容: D (rsc数值匹配) + B1 (缩量标签) + H (SS交叉校验提前)

=== 改动位置 ===
compass.py get_signal() 尾部「标签追加」段

=== 改动 1: D — 带宽收窄降级 → rsc 数值匹配 ===

原代码:
  if _F_BOLL and boll_narrow:
    tag=False
    if rtxt=="强多头":rtxt="多头";rsc=2;tag=True
    elif rtxt in ("多头","偏多"):rtxt="偏多偏弱";rsc=1;tag=True
    elif rtxt=="偏空":tag=True  # 不降级,只标签
    # 强空头/震荡类: 不处理
    if tag:labels.append("[收窄]")

改为:
  if _F_BOLL and boll_narrow:
    tag=False
    if rsc>=3:rtxt="多头";rsc=2;tag=True
    elif rsc==2:rtxt="偏多偏弱";rsc=1;tag=True
    elif rsc==-2:tag=True  # 偏空,不降级只标签
    # 强空头(rsc==-3)/震荡类: 不处理
    if tag:labels.append("[收窄]")

理由: rsc 是信号分数的数值表达 (强多头=3, 多头/偏多=2, 偏空=-2)。用数值匹配比字符串更鲁棒，以后信号文本改名不会偷偷破逻辑。

=== 改动 2: 缩量标签 (仅多头类, 不改评分) ===

在 BOLL位置标签 之前插入:
  # 缩量标签(仅多头类信号, 空头缩量正常不提示)
  if vr<0.7 and rsc>=1:
    labels.append("[缩量]")

条件说明:
  vr<0.7  → 5日均量比低于均量70%，成交显著萎缩
  rsc>=1  → 仅对偏多/多头类信号标注（空头缩量是正常现象，不标）

验证:
  中国西电 vr=0.67x 但 rsc=-1 → 正确跳过（空头缩量不提示）
  当天无股票同时满足 vr<0.7 且 rsc≥1，标签就绪待触发

=== 改动 3: H — SS 交叉校验提前到显示前 ===

位置: compass.py run_a() 函数，核心指数矩阵输出之前

原代码:
  print("  === A-Share Signal Map ===")
  risk=[];trend_score=0;...

改为 (在 print 之后、risk 之前插入交叉校验):
  print("  === A-Share Signal Map ===")
  # SS交叉校验提前到显示前 (v2.3: 防Sina实时错数据在输出层骗用户)
  _ss_sina_early=(q.get(A["SS"]) or {}).get("p")
  if _ss_sina_early is not None and syk and syk.get("c"):
    _ss_k_early=syk["c"][-1]
    if abs(_ss_sina_early-_ss_k_early)/_ss_k_early>0.01:
      if not is_trading():
        print("  ⚠️ SS数据源偏差: sina实时{:.2f} vs K线收盘{:.2f}, 展示用K线值".format(_ss_sina_early,_ss_k_early))
        _prev_close=syk["c"][-2] if len(syk["c"])>=2 else _ss_k_early
        _chg=(_ss_k_early-_prev_close)/_prev_close*100 if _prev_close>0 else 0
        q[A["SS"]]={"p":_ss_k_early,"chg":_chg}
  risk=[];trend_score=0;...

理由:
  原交叉校验在存档前执行(原L1094)，但SS值在更早的L655已显示给用户。
  Sina实时 hq.sinajs.cn 对 sh000001 偶有收盘价错误(5/21: 4174 vs 实际4077)，导致:
  → 用户看到错误的"+0.29%涨"而非实际的"-2.04%跌"
  → 基于错误显示写了[涨]的笔记
  → 第二天"昨日笔记"自然显示[涨]
  本修复将校验提前到显示前，盘中不触发(保留实时数据)，收盘后用K线值纠正。

验证逻辑:
  is_trading()=True (盘中) → 不触发，保留实时数据
  is_trading()=False (盘后/收盘) → 偏差>1%时自动替换
